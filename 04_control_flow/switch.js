// used in redux alot

switch (key) {
    case value:
        
        break;

    default:
        break;
}

// If we don't put break in our cases then following all cases are executed except DEFAULT

// In modern languages like swift there is no need to write break keyword